import 'package:flutter/material.dart';

class AppColors {
  // Primary — Slate Blue
  static const Color primary = Color(0xFF475569);
  static const Color primaryDark = Color(0xFF334155);
  static const Color primaryLight = Color(0xFFF1F5F9);

  // Accent
  static const Color accent = Color(0xFF3B82F6);

  // Status paket
  static const Color statusGudang = Color(0xFF3B82F6);
  static const Color statusAntar = Color(0xFFF59E0B);
  static const Color statusSelesai = Color(0xFF10B981);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Neutral
  static const Color bg = Color(0xFFF8FAFC);
  static const Color cardBg = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color border = Color(0xFFE2E8F0);
  static const Color divider = Color(0xFFF1F5F9);

  // Nav bar
  static const Color navActive = Color(0xFF3B82F6);

  // Hyperlink
  static const Color link = Color(0xFF2563EB);
}
